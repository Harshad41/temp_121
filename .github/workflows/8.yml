name: 3 - Create Wikis (DEV BRANCH)

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  create-empty-wikis:
    runs-on: ubuntu-latest
    steps:
      - name: Install tools
        run: sudo apt-get update -qq && sudo apt-get install -y jq git curl

      - name: Create Wikis for EMPTY Repos
        env:
          USERNAME: Harshad41
          GH_TOKEN: ${{ secrets.GH_TOKEN }}
        run: |
          echo "🔍 Scanning repositories..."

          HOME_MD=$(cat << 'EOF'
          # Welcome to the Repository Wiki
          
          This wiki contains project documentation and guides.
          
          ## Navigation
          - [Getting Started](Getting-Started)
          - [Configuration](Configuration)
          - [Troubleshooting](Troubleshooting)
          
          **Updated:** $(date +%Y-%m-%d)
          EOF
          )

          # Get repos
          repos_json=$(curl -s -H "Authorization: token $GH_TOKEN" \
            "https://api.github.com/users/$USERNAME/repos?per_page=100")

          needs_wiki=()
          echo "$repos_json" | jq -r '.[] | "\(.name),\(.has_wiki)"' | while IFS=',' read -r repo has_wiki; do
            if [[ "$has_wiki" == "false" ]]; then
              echo "📝 $repo → Wiki DISABLED (SKIPPING)"
            elif [[ "$has_wiki" == "true" ]]; then
              wiki_url="https://github.com/$USERNAME/$repo.wiki.git"
              if ! git ls-remote --exit-code --heads "$wiki_url" >/dev/null 2>&1; then
                echo "📝 $repo → Wiki EMPTY (will create)"
                needs_wiki+=("$repo")
              else
                echo "✅ $repo → Wiki exists (SKIPPING)"
              fi
            fi
          done

          echo "📊 Found ${#needs_wiki[@]} repos: ${needs_wiki[*]}"

          # CREATE WIKIS ON DEV BRANCH
          for repo in "${needs_wiki[@]}"; do
            echo ""
            echo "🔄 $repo..."
            
            TEMP_DIR=$(mktemp -d)
            cd "$TEMP_DIR"
            
            # HARDCODE DEV BRANCH
            git init
            git checkout -b dev
            
            git config user.name "GitHub Actions"
            git config user.email "actions@github.com"
            
            echo "$HOME_MD" > Home.md
            git add Home.md
            git commit -m "Add initial Home.md"
            
            git remote add origin \
              "https://$GH_TOKEN@github.com/$USERNAME/$repo.wiki.git"
            
            # PUSH TO DEV BRANCH
            if git push -u origin dev; then
              echo "✅ $repo → Wiki created on dev branch"
            else
              echo "⚠️  $repo → First push failed, retrying..."
              sleep 5
              git push -u origin dev --force || \
              echo "❌ $repo → Failed after retry"
            fi
            
            cd ..
            rm -rf "$TEMP_DIR"
          done

          echo ""
          echo "🎉 COMPLETE!"
